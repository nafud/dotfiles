# Read by git alongside ~/.gitconfig (both are loaded; ~/.gitconfig,
# which holds the machine's identity, wins on conflict) — linked with
# the rest of config/, so it is live on edit. Only what the workspace
# brings: delta as the pager.
[core]
	pager = delta
[interactive]
	diffFilter = delta --color-only
[delta]
	navigate = true
