# Read by the systemd user manager at login; niri runs as its service,
# so every process niri spawns (terminals, the bar's on-click popups)
# inherits this. newt apps — nmtui first — take the mono palette in
# config/newt/colors instead of their blue default.
NEWT_COLORS_FILE=${HOME}/.config/newt/colors
