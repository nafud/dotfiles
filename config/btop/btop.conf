#? Config file for btop v. 1.3+ — kept minimal on purpose: btop rewrites
#? this file with its full settings on every exit (that is btop behavior,
#? not an error); commit or discard the rewrite in the repo as desired.

#* Let the terminal background show through, so btop inherits the
#* window's glass instead of painting an opaque ground.
theme_background = False
