# sshd is off by default (setup.sh disables it); started by hand it is
# key-only, and the firewall (/etc/nftables.conf) admits it from the
# LAN alone. KbdInteractiveAuthentication is already off in the
# package's 99-archlinux.conf.
PasswordAuthentication no
PermitRootLogin no
