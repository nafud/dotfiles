# The persistent journal is capped at 1G; without a cap journald
# takes up to 10% of the filesystem (4G at most) and this root is the
# snapshotted @ subvolume, so old logs would live on in every snapshot
# (journald.conf(5); Arch Wiki, Systemd/Journal: "Journal size limit").
[Journal]
SystemMaxUse=1G
