# plymouth quits once multi-user.target is reached; --retain-splash
# leaves its last frame on the display instead of restoring the text
# console, so the ring stays up until the greeter's compositor paints
# over it. greetd is ordered after plymouth-quit-wait upstream.
[Service]
ExecStart=
ExecStart=-/usr/bin/plymouth quit --retain-splash
