# The mono theme (system/usr/share/plymouth/themes/mono), drawn as soon
# as plymouthd is up — ShowDelay 0 is the default, stated so the intent
# is on record. The mkinitcpio hook reads Theme= here to pack the theme
# and its font into the initramfs.
[Daemon]
Theme=mono
ShowDelay=0
